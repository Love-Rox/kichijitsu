{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "kichijitsu デスクトップシェルの既定 capability。増分1はリモート URL を読むだけで Tauri コマンドを呼ばないため core:default のみで足りる。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
